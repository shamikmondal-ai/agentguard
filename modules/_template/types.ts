// ── TEMPLATE: Replace these types with your module's specific data shapes ──

export interface TemplateTestInput {
  prompt: string
}

export interface TemplateTestResult {
  inputId: string
  passed: boolean
  detail: string
}
